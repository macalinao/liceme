var React = require('react');

var Landing = require('./Landing.jsx');

var App = React.createClass({
  render: function() {
    return (
      <div><Landing /></div>
    );
  }
});

module.exports = App;
